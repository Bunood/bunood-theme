[pre_model_sync]
# Patches here run BEFORE doctypes are migrated.

[post_model_sync]
# Patches here run AFTER doctypes are migrated.
# This one runs before after_migrate -> _seed_defaults, which is exactly why it
# guards on tabSingles row-absence rather than on a value: seeding first would
# make the patch look as though it had already run.
bunood_theme.patches.v0_11_0.chrome_placement
bunood_theme.patches.v0_11_0.split_quick_links
bunood_theme.patches.v0_11_0.status_is_a_component
# The container split (slice 2c), one patch per container. Each answers only
# for its own field, from what the site's layout RENDERED. One patch writing
# all five would have to be right about all five at once, and could not later
# be re-read to find out what any single one of them decided.
bunood_theme.patches.v0_11_0.container_topbar
bunood_theme.patches.v0_11_0.container_pagehead
bunood_theme.patches.v0_11_0.container_dock
bunood_theme.patches.v0_11_0.container_sidepane
bunood_theme.patches.v0_11_0.container_bottombar
# E1: every placement moves onto "<Region> <Zone>". Runs AFTER the container
# patches because it does not depend on them, and last is where a vocabulary
# change belongs — the ones above still speak the old one.
bunood_theme.patches.v0_11_0.slot_vocabulary
# Item 7: "Operator" is ERPNext's word for a machine operator — a person — in
# Manufacturing, and Frappe's dictionary is one flat global map, so the word
# cannot also name a sidebar preset and a status style. Two fields, two
# different replacements, because they never meant the same thing.
bunood_theme.patches.v0_11_0.rename_operator
# Item 23: the icon settings moved out of the sidebar and breadcrumb kits into
# one Icons axis, renaming four fields. This carries each existing site's stored
# value onto its new fieldname so the icon appearance survives the rename.
bunood_theme.patches.v0_15_0.rename_icon_fields
# Item 35: claim Print Settings.print_style once for existing sites. v16
# defaults it to "Redesign", a name the installer's vacancy tuple did not
# carry, so the Bunood Print Style had been installed everywhere and applied
# NOWHERE since 0.13.0. One-time by construction (patches run once); the
# ongoing claim for fresh installs is the widened STOCK_STYLES in
# printing/install.py. The honest cost is in the patch's docstring.
bunood_theme.patches.v0_35_0.claim_print_style
# Item 36: the two axis-field renames build.mjs's KNOWN VIOLATIONS block had
# promised to "the component rework's patch" since the rework itself
# (enable_command_palette -> palette_enabled, default_density ->
# density_default). Carries each existing site's stored value onto its new
# fieldname; the v0_15_0 icon rename is the copied precedent.
bunood_theme.patches.v0_36_0.rename_axis_fields
# Item 37: the last stored preset NAMES go. A preset writes values and stops
# existing; the label is derived by comparison, which is what the settings
# architecture has said since phase 0.
bunood_theme.patches.v0_37_0.migrate_user_sidebar_preset
bunood_theme.patches.v0_37_0.drop_sidebar_preset
bunood_theme.patches.v0_37_0.drop_desk_layout

# Item 38: the Appearance dialog subsumes the density menu row. Must land with
# the seeder rename, or after_migrate re-creates it.
bunood_theme.patches.v0_38_0.drop_density_navbar_item

# Item 40: the apps rail retires and its capability moves to the dock. Must run
# after the doctype sync (it reads the departed field's row directly, because
# get_single_value raises for a field the meta has lost).
bunood_theme.patches.v0_40_0.retire_apps_rail

# Same release, same reason: the two renamed side-pane fields. A Single's
# values are keyed by FIELDNAME, so a rename orphans the stored row and the
# site silently loses a setting it chose. Nothing errors, which is why it is
# easy to skip.
bunood_theme.patches.v0_40_0.rename_sidebar_fields

# And the glass merge, same release. Reads both departed fields' rows
# directly for the same reason, and only ever writes when the tenant chose
# Glass with a full blur - every other combination is already expressed by
# the material it keeps.
bunood_theme.patches.v0_40_0.merge_glass_material

# And the last of the side-pane field model: two active-link styles that
# rendered as another one, and three fields with nothing behind them. The
# style rewrite matters most - an out-of-range Select on a Single fails
# validation for the WHOLE document, so one stale value breaks every later
# save of every other setting.
bunood_theme.patches.v0_40_0.collapse_pane_options

# The Place row replaces four rows of pane furniture with one, so the two
# quick links stand down by default. Only a row still holding the OLD default
# moves: both fields are THEME AXES, and leaving them would make every site
# read Custom on all twelve theme cards against a baseline that had moved.
bunood_theme.patches.v0_40_0.quick_links_stand_down

# The option vocabulary the plan retired, in one pass: Accordion Cards and
# Mini-Cards become Cards, Button Only stops being a trigger (and gives back
# the edge button its implementation was drawing anyway), Top becomes Header
# and Bottom moves off the foot's own controls.
bunood_theme.patches.v0_40_0.retune_pane_vocabulary

# And the two whose DEFAULT moved rather than whose options did: the rail mode
# loses Manual Collapse (the user's call), and the pane's icons lose their chip
# for the dock's size. Both are theme axes, so both move only where the site
# still holds the value that is going away.
bunood_theme.patches.v0_40_0.retire_manual_collapse

# Last, and it stays: whatever the translation above could not map, this makes
# legal. An out-of-range Select on a Single fails validation for the WHOLE
# document, so one stale placement silently breaks every later save of every
# other setting — measured 2026-08-08, six unrelated checks red.
bunood_theme.patches.v0_11_0.heal_unknown_placements
